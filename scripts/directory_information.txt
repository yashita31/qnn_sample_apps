Automation
Setup: automatically install SDK and other dependencies (this isn't necessary, newer versions of onnxruntime have QNN dependencies)
.git hooks to force unit testing prior to pushing to main branch
Deployment Scripts
